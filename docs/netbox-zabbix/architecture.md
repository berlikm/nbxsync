# Architecture

The mental model (three layers, winning configuration group, merging Template Rules) and the live mapping live in one place:

→ [`configuration.md`](configuration.md) — start at **How this works**, then the numbered GUI sections.
